apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: deployment-role
rules:
- apiGroups: ["", "apps", "extensions"]
  resources: ["deployments", "replicasets", "pods"]
  verbs: ["apply", "get", "list", "watch", "create", "update", "patch", "delete"]
